import { makeStyles } from '@material-ui/core';

export default makeStyles(theme => ({
  detailContainer: {
    display: 'flex',
    justifyContent: 'center',
  },
  movieDetail: {
    width: 500,
  },
  plot: {
    padding: 24,
  },
}));
